{{#>base page="category"}}
  {{#>layouts/single-column}}
    <header {{region "header"}}>
      {{>components/header}}
    </header>

    <main {{region "main"}}>
      {{> @partial-block }}
      {{>components/posts posts=categoryPosts}}
    </main>
  {{/layouts/single-column}}
{{/base}}
